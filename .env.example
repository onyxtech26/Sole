# Optional build-time overrides. The app falls back to the SOLE project's
# published anon credentials when these are unset, so a plain `npm run dev`
# works with no setup.

VITE_SUPABASE_URL=https://jpsyrostafidkneqflui.supabase.co
VITE_SUPABASE_ANON_KEY=
